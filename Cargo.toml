[package]
name = "watchdog"
version = "0.1.0"
edition = "2021"

[features]
serde = ["dep:serde"]

[dev-dependencies]
tokio-test = "0.4.2"

[dependencies]
anyhow = "1.0.71"
serde = { version = "1.0.164", features = ["derive"], optional = true }
tokio = { version = "1.28.2", features = ["full"] }
